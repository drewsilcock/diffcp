from distutils.core import setup

setup(name="diffcp",
      version="1.0",
      description="Copy unique files only.",
      author="Drew Silcock",
      author_email="drew@drewsilcock.co.uk",
      packages=["diffcp"],
      )
